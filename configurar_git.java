📂 Iniciar uma área de trabalho

clone → Clonar um repositório existente para um novo diretório.

init → Criar um repositório Git vazio ou reinicializar um já existente.
-----------------------------------------------------------------------------------------------------------------------------------------

✏️ Trabalhar nas mudanças atuais

add → Adicionar o conteúdo de arquivos na área de stage (preparação para commit).

mv → Mover ou renomear arquivos, diretórios ou links simbólicos.

restore → Restaurar arquivos da árvore de trabalho (voltar ao estado salvo no Git).

rm → Remover arquivos da árvore de trabalho e também da área de stage.
-----------------------------------------------------------------------------------------------------------------------------------------

🔎 Examinar histórico e estado

bisect → Usar busca binária para encontrar o commit que introduziu um bug.

diff → Mostrar diferenças entre commits, entre commit e árvore de trabalho, etc.

grep → Mostrar linhas que correspondem a um padrão de busca.

log → Exibir o histórico de commits.

show → Exibir informações sobre objetos variados (commits, tags, blobs).

status → Mostrar o estado atual da árvore de trabalho (arquivos modificados, staged, etc).
-----------------------------------------------------------------------------------------------------------------------------------------

🌳 Crescer, marcar e ajustar o histórico

backfill → Baixar objetos faltando em um clone parcial.

branch → Listar, criar ou deletar branches.

commit → Registrar as mudanças no repositório.

merge → Unir dois ou mais históricos de desenvolvimento.

rebase → Reaplicar commits em cima de outro ponto de base.

reset → Resetar o HEAD atual para um estado específico (pode descartar alterações).

switch → Trocar entre branches.

tag → Criar, listar, deletar ou verificar tags (com suporte a GPG).
-----------------------------------------------------------------------------------------------------------------------------------------

🤝 Colaborar

fetch → Baixar objetos e referências de outro repositório remoto (não altera o branch atual).

pull → Fazer fetch + integrar (merge/rebase) no branch local.

push → Enviar commits/objetos locais para o repositório remoto.